# RateLimiter (Moving Window Solution)

How to try it?

- Run `docker-compose up --build` to start docker redis
- Run `curl -v -H "X-API-Key: testapikey1" localhost:5000/rate_limiting` to test `rate_limiting` api
